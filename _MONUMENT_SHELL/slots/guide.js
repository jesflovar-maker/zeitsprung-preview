/* _MONUMENT_SHELL/slots/guide.js — SLOT 9: GUIDE
   ----------------------------------------------------------------------------
   SCAFFOLDING ONLY — not wired into any page. NO reusable reference
   implementation exists yet for this slot anywhere in the project — every
   monument's `featureFlags.guide` is `false` ("no guided-tour module exists
   yet", per PLATFORM_ARCHITECTURE_V3.md Section F). This stub exists only to
   reserve the slot's position/name in the shared shell contract for whenever
   a guided-tour module is designed.

   Expected `config` shape: intentionally undefined until the Guide module
   design exists (out of scope for this phase).
*/
export function renderGuideSlot(config, container) {
  // eslint-disable-next-line no-console
  console.warn("[_MONUMENT_SHELL] renderGuideSlot() is a scaffold stub — not implemented (no reference module exists anywhere yet).", { config, container });
  return null;
}
