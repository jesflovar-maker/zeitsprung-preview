/* _MONUMENT_SHELL/slots/media.js — SLOT 8: MEDIA / REEL
   ----------------------------------------------------------------------------
   SCAFFOLDING ONLY — not wired into any page. See ../index.js for the full
   contract. Reusable reference implementation to MODEL against:
   STEINERNE_BRUECKE/index.html's `#historicalReel` + main.js/video-playback.js
   (safePlay/safePause) — a poster-first, lazy-loaded reel video with race-
   safe play/pause handling.

   Expected `config` shape a future monument would author (illustrative only):
     { reelVideo: "assets/<monument>/reel/....mp4", reelPoster: "....jpg" }
*/
export function renderMediaSlot(config, container) {
  // eslint-disable-next-line no-console
  console.warn("[_MONUMENT_SHELL] renderMediaSlot() is a scaffold stub — not implemented.", { config, container });
  return null;
}
