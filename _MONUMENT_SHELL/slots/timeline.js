/* _MONUMENT_SHELL/slots/timeline.js — SLOT 5: TIMELINE
   ----------------------------------------------------------------------------
   SCAFFOLDING ONLY — not wired into any page. NO reusable reference
   implementation exists yet for this slot in STEINERNE_BRUECKE — its own
   `featureFlags.timeline` is `false` ("still 'VISUAL PRODUCTION' placeholder
   per RELEASE_STATUS.md/orchestrator ground truth", per
   PLATFORM_ARCHITECTURE_V3.md Section F). This stub exists only to reserve
   the slot's position/name in the shared shell contract for whenever the
   Timeline module itself is designed — it has no functional model to follow.

   Expected `config` shape: intentionally undefined until the Timeline
   module design exists (out of scope for this phase).
*/
export function renderTimelineSlot(config, container) {
  // eslint-disable-next-line no-console
  console.warn("[_MONUMENT_SHELL] renderTimelineSlot() is a scaffold stub — not implemented (no reference module exists yet, even for Steinerne Brücke).", { config, container });
  return null;
}
