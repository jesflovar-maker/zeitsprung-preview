/* _MONUMENT_SHELL/slots/facts.js — SLOT 2: FACTS
   ----------------------------------------------------------------------------
   SCAFFOLDING ONLY — not wired into any page. See ../index.js for the full
   contract. Reusable reference implementation to MODEL against: main.js's
   renderFacts() + index.html's `#facts` — a short, scannable list of
   quick key facts (dates, dimensions, status) shown right after the intro.

   Expected `config` shape a future monument would author (illustrative only):
     { facts: [ { label: {de,en,es}, value: {de,en,es} }, ... ] }
*/
export function renderFactsSlot(config, container) {
  // eslint-disable-next-line no-console
  console.warn("[_MONUMENT_SHELL] renderFactsSlot() is a scaffold stub — not implemented.", { config, container });
  return null;
}
